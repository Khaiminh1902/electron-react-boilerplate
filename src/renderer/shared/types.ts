// Define a type for the slice state
export interface IMainState {
    value: number
}

export interface IServerBtn {
    icon?: any; 
    img?: string; 
    title: string; 
    url?: string;
    onClick?: any;
}